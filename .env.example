# SwimVault 환경변수 예시 — `.env`로 복사해 사용. (.env는 .gitignore 처리됨)

# 인증 토큰 서명 (auth-service / api-gateway 공유)
JWT_SECRET=dev-secret

# LLM 키 — extraction-service에만 사용 (R5). 비우면 결정적 mock 추출로 동작.
ANTHROPIC_API_KEY=
